targets:
  $default:
    sources:
      include:
        - lib/**
        - test/**
        - $package$
    builders:
      drift_dev:
        options:
          store_date_time_values_as_text: true
          fatal_warnings: true
